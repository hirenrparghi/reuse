name: myworkflow
on: 
 workflow_call:
   outputs:
       new_value:
         description: "The first output string"
         value: ${{ jobs.deploy.outputs.output1 }}  
jobs:  
 deploy:
  runs-on: ubuntu-latest
  outputs:
      output1: ${{ steps.set_env.outputs.new_value }}
  steps:
    - name: Set Environment Variables
      id: set_env
      run : |
        echo "new_value=$(date)" >> $GITHUB_ENV
    - name: get env
      run: |
       echo "${{ env.new_value }}"
      
